services:
  - type: web
    name: haya-portal
    env: node
    region: singapore
    plan: free
    buildCommand: npm install
    startCommand: node server/server.js
    envVars:
      - key: PORT
        value: 3000
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        sync: false

